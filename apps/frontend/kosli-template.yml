version: 1
trail:
    attestations:
        - name: pull-request
          type: pull_request
        - name: veracode-executed
          type: custom:veracode-executed
    artifacts:
        - name: frontend

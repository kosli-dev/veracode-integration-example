version: 1
trail:
  attestations:
    - name: pull-request
      type: pull_request
  artifacts:
    - name: frontend
      attestations:
        - name: security-scan-executed
          type: custom:veracode-scan-executed
